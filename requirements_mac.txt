# macOS-specific requirements for WebRTCHub
# Windows-only packages (e.g. dxcam) have been removed.
# Extra macOS dependencies for PyAutoGUI have been added.

opencv-python>=4.5.0
numpy>=1.20.0
mss>=9.0.1
pillow>=9.0.0
pyaudio>=0.2.13  # may require `brew install portaudio` before pip
aiortc>=1.6.0
av>=12.0
aiohttp>=3.9.5
miniupnpc
natpmp
GPUtil
pyautogui
# Additional packages required by PyAutoGUI on macOS
pyobjc-core>=10.1
pyobjc-framework-Quartz>=10.1
psutil>=5.9.0
